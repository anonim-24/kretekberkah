// Contoh script untuk interaksi di website, jika diperlukan.
// Seperti animasi atau validasi form.

document.querySelector('form').addEventListener('submit', function (event) {
    alert("Form berhasil dikirim!");
});
